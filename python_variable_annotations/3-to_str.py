#!/usr/bin/env python3
'''Function takes a float as argument
    and returns the string representation
    of that float
'''


def to_str(n: float) -> str:
    '''N will be passed as float, converted
    to a string using str() class and returned using
    annotation to hint typing'''

    convertedString = str(n)
    return convertedString
